node_modules
# Keep environment variables out of version control
.env

# Build output — generated by `npm run build`, never committed.
# Production builds the frontend on the server (frontend/node_modules is
# present there), so a committed dist would only ever go stale.
dist
