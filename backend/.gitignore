node_modules
# Keep environment variables out of version control
.env
.env.test

# Scratch upload dir used by tests/purge.test.js against the disposable
# local test DB (never committed, never touches production storage)
/tests/tmp-uploads

/generated/prisma

/uploads
/logs

# Debug/scratch artifacts that shouldn't be committed (VAPT L-4)
clone_debug.json
*.out.log
drop_schema.sql
.DS_Store
